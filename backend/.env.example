# Copy to .env and fill in values before running the server.
# Phase 1 has no required API keys — all models run locally.

# Future: add any third-party API keys here, e.g.
# SOME_API_KEY=your_key_here
