# Python 3.9  |  Install PyTorch separately via conda (see README.md)

# API framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Face detection + alignment
insightface>=0.7.3
onnxruntime>=1.17.3          # CPU; replace with onnxruntime-gpu on CUDA machines

# Image / array
numpy>=1.24.0,<2.0           # insightface requires numpy <2
opencv-python>=4.9.0
Pillow>=10.0.0

# 3D mesh loading + normals
trimesh>=4.0.0

# GLB / GLTF assembly with morph targets
pygltflib>=1.16.0

# Draco mesh compression (required by spec; install separately if pip fails)
# Linux/macOS:  pip install DracoPy
# Windows:      pip install DracoPy --find-links https://github.com/seung-lab/DracoPy/releases
DracoPy>=1.4.0

# Coefficient file I/O (multi-image averaging)
scipy>=1.11.0

# Config
python-dotenv>=1.0.0
